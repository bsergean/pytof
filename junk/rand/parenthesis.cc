int
main()
{
    std::string valid1("int main() { return args[3]; }");
    std::string valid2("int main();");

    // std::string invalid1("int main() { return args[3]; }");
    std::cout << correct(
    
            irene est une super petite fille qui fait des prouts.
}

bool
correct(const std::string& str)
{
    int parenthesisCounter = 0;
    int bracesCounter      = 0;
    int bracketCounter     = 0;

    for (int i = 0; i < str.size(); ++i) {

    }
}
